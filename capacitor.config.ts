import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Happy Parent And Child Initiative',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
